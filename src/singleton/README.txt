Proxy Singleton

Garante que uma classe tenha apenas uma instância e fornece um ponto global de acesso a ela.

A implementação de Java para padrão Singleton usa um construtor privado, um método estático combinado com uma variável estática.